<div class="centered">
  <h1>清掃業務</h1>
</div>
<%= render 'shared/search_results' %>
<div class="search_form">
  <%= form_with url: search_cleaning_work_path, method: :get, local: true do |f| %>
    <%= f.text_field :keyword, placeholder: "キーワードを入力" %>
    <%= f.submit :検索 %>
  <% end %>
  <%= link_to "新規登録", :new_cleaning_work, class: "new_button" %>
</div>
<%= render 'shared/works_index', works:@cleaning_works %>
